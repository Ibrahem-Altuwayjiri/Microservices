@model List<Services.Frontend.Web.Services.DTOs.LookupService.DomainsDto>

<div class="admin-services-container">
    <div class="row mb-4">
        <div class="col-md-6">
            <h3 class="text-primary">Domains Management</h3>
        </div>
    </div>

    <div class="table-responsive">
        <table class="table table-hover table-striped">
            <thead class="table-dark">
                <tr>
                    <th>ID</th>
                    <th>Name (AR)</th>
                    <th>Name (EN)</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody id="domainsTable">
                @if (Model != null && Model.Count > 0)
                {
                    @foreach (var domain in Model)
                    {
                        <tr>
                            <td>@domain.Id</td>
                            <td>@domain.NameAr</td>
                            <td>@domain.NameEn</td>
                            <td>
                                <button class="btn btn-sm btn-primary" data-action="edit" data-id="@domain.Id">
                                    <i class="fas fa-edit"></i> Edit
                                </button>
                                <button class="btn btn-sm btn-danger" data-action="delete" data-id="@domain.Id">
                                    <i class="fas fa-trash"></i> Delete
                                </button>
                            </td>
                        </tr>
                    }
                }
                else
                {
                    <tr>
                        <td colspan="4" class="text-center text-muted">No domains found</td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
