@model List<Services.Frontend.Web.Services.DTOs.LookupService.HeaderDto>

<div class="admin-services-container">
    <div class="row mb-4">
        <div class="col-md-6">
            <h3 class="text-primary">Headers Management</h3>
        </div>
    </div>

    <div class="table-responsive">
        <table class="table table-hover table-striped">
            <thead class="table-dark">
                <tr>
                    <th>ID</th>
                    <th>Name (AR)</th>
                    <th>Name (EN)</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody id="headersTable">
                @if (Model != null && Model.Count > 0)
                {
                    @foreach (var header in Model)
                    {
                        <tr>
                            <td>@header.Id</td>
                            <td>@header.NameAr</td>
                            <td>@header.NameEn</td>
                            <td>
                                <button class="btn btn-sm btn-primary" data-action="edit" data-id="@header.Id">
                                    <i class="fas fa-edit"></i> Edit
                                </button>
                                <button class="btn btn-sm btn-danger" data-action="delete" data-id="@header.Id">
                                    <i class="fas fa-trash"></i> Delete
                                </button>
                            </td>
                        </tr>
                    }
                }
                else
                {
                    <tr>
                        <td colspan="4" class="text-center text-muted">No headers found</td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
