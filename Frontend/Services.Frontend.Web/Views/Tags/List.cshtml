@model List<Services.Frontend.Web.Services.DTOs.LookupService.TagsDto>

<div class="admin-services-container">
    <div class="row mb-4">
        <div class="col-md-6">
            <h3 class="text-primary">Tags Management</h3>
        </div>
    </div>

    <div class="table-responsive">
        <table class="table table-hover table-striped">
            <thead class="table-dark">
                <tr>
                    <th>ID</th>
                    <th>Name (AR)</th>
                    <th>Name (EN)</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody id="tagsTable">
                @if (Model != null && Model.Count > 0)
                {
                    @foreach (var tag in Model)
                    {
                        <tr>
                            <td>@tag.Id</td>
                            <td>@tag.NameAr</td>
                            <td>@tag.NameEn</td>
                            <td>
                                <button class="btn btn-sm btn-primary" data-action="edit" data-id="@tag.Id">
                                    <i class="fas fa-edit"></i> Edit
                                </button>
                                <button class="btn btn-sm btn-danger" data-action="delete" data-id="@tag.Id">
                                    <i class="fas fa-trash"></i> Delete
                                </button>
                            </td>
                        </tr>
                    }
                }
                else
                {
                    <tr>
                        <td colspan="4" class="text-center text-muted">No tags found</td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
